# Wallet Lace and 1AM dapp-connector-api 4.x

Discovery window.midnight[walletId] InitialAPI. Connect initial.connect preprod -> ConnectedAPI. Services via getConfiguration indexerUri indexerWsUri proverServerUri. Proving via getProvingProvider. Balancing via balanceUnsealedTransaction. Submit via submitTransaction. v2 merchantCoinPk is coin public key hex 32B.
